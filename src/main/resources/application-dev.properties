# 開発用プロファイル（--spring.profiles.active=dev）
# - GET /applications と GET /applications/api/applications を未ログインで許可
# - DB 接続に失敗した場合も一覧は空データで返す
#
# プロファイルを付けずに一覧だけ見る場合は application.properties の
# jobapp.preview-list-without-auth=true を利用
#
# 本番では dev を有効にせず、preview プロパティも false のままにしてください。
